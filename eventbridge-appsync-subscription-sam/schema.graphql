type Update @aws_iam {
  message: String!
}

type Mutation {
  onEbUpdate (
    message: String!
  ): Update @aws_iam
}

type Subscription {
  onEbUpdate(message: String): Update
  @aws_subscribe(mutations: ["onEbUpdate"])
}

type Query {
  getUpdate(): Update
}